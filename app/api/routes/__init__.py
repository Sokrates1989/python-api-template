"""
Legacy route package retained for compatibility imports.

New product routes belong under ``apps.<app_id>.routes``. Reusable shared route
groups belong under ``api.shared_routes`` and must be explicitly selected by
backend app definitions.
"""
