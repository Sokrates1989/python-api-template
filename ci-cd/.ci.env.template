# CI/CD Environment Configuration Template
# Copy this file to .ci.env and commit it
# Only non-sensitive build parameters belong here.

# Docker image version/tag used by the pipeline
# Update this value whenever you cut a new release.
IMAGE_VERSION=0.1.0
