# Python version for Docker
PYTHON_VERSION=3.13

# Database Configuration - Neo4j (Local Docker)
DB_TYPE=neo4j
DB_MODE=local

# =============================================================================
# LOCAL DEVELOPMENT CREDENTIALS - Neo4j
# =============================================================================
# ⚠️  These are DEFAULT credentials for local Docker development only!
# ⚠️  For production, change these to strong, unique passwords!
#
# Neo4j default credentials:
# - Username: neo4j (this is the default and typically doesn't change)
# - Password: password (MUST be changed for production!)
#
# How to change credentials:
# 1. Update DB_USER and DB_PASSWORD below
# 2. Delete .docker/neo4j-data/ folder to recreate database with new credentials
# 3. Restart: docker compose -f docker-compose.neo4j.yml up --build
#
# Production security:
# - Use Docker secrets or environment variables from secure vaults
# - Never commit production credentials to version control
# - Use strong passwords (20+ characters, mixed case, numbers, symbols)
# - Consider using Neo4j Enterprise with LDAP/SSO authentication
# =============================================================================

# Local Docker Neo4j (uses docker-compose.neo4j.yml)
NEO4J_URL=bolt://neo4j:7687
DB_USER=neo4j           # Used by docker-compose to create Neo4j user
DB_PASSWORD=password    # ⚠️  CHANGE THIS for production!

# API Configuration
PORT=8000
DEBUG=true

# Redis Configuration
REDIS_URL=redis://redis:6379

# Docker Image Tag
IMAGE_TAG=local-neo4j
