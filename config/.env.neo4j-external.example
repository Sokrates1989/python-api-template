# Python version for Docker
PYTHON_VERSION=3.13

# Database Configuration - Neo4j (External)
DB_TYPE=neo4j
DB_MODE=external

# =============================================================================
# EXTERNAL DATABASE CREDENTIALS - Neo4j
# =============================================================================
# ⚠️  SECURITY WARNING: External database credentials!
# ⚠️  These connect to your production/staging Neo4j server!
#
# Neo4j connection requires:
# - NEO4J_URL: bolt:// or neo4j:// protocol (use neo4j+s:// for SSL)
# - DB_USER: Neo4j username (default: neo4j)
# - DB_PASSWORD: Neo4j password (MUST be changed from default!)
#
# Production security best practices:
# - NEVER commit this file with real credentials to version control
# - Use environment variables from secure vaults (AWS Secrets Manager, Azure Key Vault)
# - Use Docker secrets: https://docs.docker.com/engine/swarm/secrets/
# - Use SSL/TLS connections (neo4j+s:// protocol)
# - Consider Neo4j Enterprise with LDAP/SSO authentication
# - Use read-only credentials for read-only operations
# - Rotate passwords regularly
# =============================================================================

# External Neo4j (connects to existing database)
# Update these values to point to your external Neo4j server
# For SSL: neo4j+s://your-neo4j-server.com:7687
NEO4J_URL=bolt://your-neo4j-server.com:7687
DB_USER=neo4j               # Neo4j username (typically 'neo4j')
DB_PASSWORD=your_password   # ⚠️  Use secure vault! Don't hardcode production passwords!

# API Configuration
PORT=8000
DEBUG=true

# Redis Configuration
# For external Redis, update this URL
REDIS_URL=redis://localhost:6379

# Docker Image Tag
IMAGE_TAG=external-neo4j
