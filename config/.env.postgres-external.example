# Python version for Docker
PYTHON_VERSION=3.13

# Database Configuration - PostgreSQL (External)
DB_TYPE=postgresql
DB_MODE=external

# =============================================================================
# EXTERNAL DATABASE CREDENTIALS - PostgreSQL
# =============================================================================
# ⚠️  SECURITY WARNING: External database credentials!
# ⚠️  These connect to your production/staging PostgreSQL server!
#
# For PostgreSQL, credentials can be provided in two ways:
# 1. In DATABASE_URL (recommended): postgresql://user:password@host:port/dbname
# 2. Separately as DB_USER and DB_PASSWORD (optional, for reference)
#
# Production security best practices:
# - NEVER commit this file with real credentials to version control
# - Use environment variables from secure vaults (AWS Secrets Manager, Azure Key Vault)
# - Use Docker secrets: https://docs.docker.com/engine/swarm/secrets/
# - Consider using IAM authentication for cloud databases
# - Use read-only credentials for read-only operations
# - Rotate passwords regularly
# - Use SSL/TLS connections (add ?sslmode=require to DATABASE_URL)
# =============================================================================

# External PostgreSQL (connects to existing database)
# Update these values to point to your external PostgreSQL server
# Example with SSL: postgresql://user:password@host:5432/dbname?sslmode=require
DATABASE_URL=postgresql://your_user:your_password@your-postgres-server.com:5432/your_database
DB_HOST=your-postgres-server.com
DB_NAME=your_database
DB_USER=your_user         # Optional: Only needed if not using DATABASE_URL
DB_PASSWORD=your_password # ⚠️  Use secure vault! Don't hardcode production passwords!
DB_PORT=5432

# API Configuration
PORT=8000
DEBUG=true

# Redis Configuration
# For external Redis, update this URL
REDIS_URL=redis://localhost:6379

# Docker Image Tag
IMAGE_TAG=external-postgres
