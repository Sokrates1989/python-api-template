# Python version for Docker
PYTHON_VERSION=3.13

# Database Configuration - PostgreSQL (Local Docker)
DB_TYPE=postgresql
DB_MODE=local

# =============================================================================
# LOCAL DEVELOPMENT CREDENTIALS - PostgreSQL
# =============================================================================
# ⚠️  These are DEFAULT credentials for local Docker development only!
# ⚠️  For production, change these to strong, unique passwords!
#
# How to change credentials:
# 1. Update DB_USER and DB_PASSWORD below
# 2. Update the same credentials in DATABASE_URL
# 3. Delete .docker/postgres-data/ folder to recreate database with new credentials
# 4. Restart: docker compose -f docker-compose.postgres.yml up --build
#
# Production security:
# - Use Docker secrets or environment variables from secure vaults
# - Never commit production credentials to version control
# - Use strong passwords (20+ characters, mixed case, numbers, symbols)
# =============================================================================

# Local Docker PostgreSQL (uses docker-compose.postgres.yml)
# You can either set DATABASE_URL or set individual components (DB_HOST, DB_USER, etc.)
# The application will build the URL automatically from components if DATABASE_URL is not set
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/apidb
DB_HOST=postgres        # Docker service name
DB_NAME=apidb
DB_USER=postgres        # Used by docker-compose to create PostgreSQL user
DB_PASSWORD=postgres    # ⚠️  CHANGE THIS for production!
DB_PORT=5432

# API Configuration
PORT=8000
DEBUG=true

# Redis Configuration
REDIS_URL=redis://redis:6379

# Docker Image Tag
IMAGE_TAG=local-postgres
