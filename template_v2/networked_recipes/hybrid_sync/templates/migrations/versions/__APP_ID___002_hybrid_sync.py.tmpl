"""Add generated owner-scoped hybrid synchronization tables."""

from __future__ import annotations

from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql


revision = "__APP_ID___002_hybrid_sync"
down_revision = "__APP_ID___001_records"
branch_labels = None
depends_on = None


def upgrade() -> None:
    """Create entity, immutable change-feed, and idempotency tables.

    Returns:
        None after the app-scoped tables and indexes exist.
    """

    op.create_table(
        "__APP_ID___sync_entities",
        sa.Column("owner_subject", sa.String(length=255), nullable=False),
        sa.Column("entity_type", sa.String(length=48), nullable=False),
        sa.Column("entity_id", sa.String(length=128), nullable=False),
        sa.Column("version", sa.Integer(), nullable=False),
        sa.Column("payload", postgresql.JSONB(astext_type=sa.Text()), nullable=True),
        sa.Column("deleted", sa.Boolean(), nullable=False),
        sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
        sa.PrimaryKeyConstraint("owner_subject", "entity_type", "entity_id"),
    )
    op.create_index(
        "ix___APP_ID___sync_entity_owner_updated",
        "__APP_ID___sync_entities",
        ["owner_subject", "updated_at"],
    )
    op.create_table(
        "__APP_ID___sync_changes",
        sa.Column("sequence", sa.BigInteger(), autoincrement=True, nullable=False),
        sa.Column("owner_subject", sa.String(length=255), nullable=False),
        sa.Column("entity_type", sa.String(length=48), nullable=False),
        sa.Column("entity_id", sa.String(length=128), nullable=False),
        sa.Column("version", sa.Integer(), nullable=False),
        sa.Column("payload", postgresql.JSONB(astext_type=sa.Text()), nullable=True),
        sa.Column("deleted", sa.Boolean(), nullable=False),
        sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False),
        sa.PrimaryKeyConstraint("sequence"),
    )
    op.create_index(
        "ix___APP_ID___sync_change_owner_sequence",
        "__APP_ID___sync_changes",
        ["owner_subject", "sequence"],
    )
    op.create_table(
        "__APP_ID___sync_operations",
        sa.Column("id", sa.BigInteger(), autoincrement=True, nullable=False),
        sa.Column("owner_subject", sa.String(length=255), nullable=False),
        sa.Column("operation_id", sa.String(length=96), nullable=False),
        sa.Column("request_sha256", sa.String(length=64), nullable=False),
        sa.Column("result", postgresql.JSONB(astext_type=sa.Text()), nullable=False),
        sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
        sa.PrimaryKeyConstraint("id"),
        sa.UniqueConstraint(
            "owner_subject",
            "operation_id",
            name="uq___APP_ID___sync_operation_owner_id",
        ),
    )
    op.create_index(
        "ix___APP_ID___sync_operation_owner",
        "__APP_ID___sync_operations",
        ["owner_subject"],
    )


def downgrade() -> None:
    """Remove only the generated hybrid synchronization tables.

    Returns:
        None after indexes and tables are removed in dependency-safe order.
    """

    op.drop_index(
        "ix___APP_ID___sync_operation_owner",
        table_name="__APP_ID___sync_operations",
    )
    op.drop_table("__APP_ID___sync_operations")
    op.drop_index(
        "ix___APP_ID___sync_change_owner_sequence",
        table_name="__APP_ID___sync_changes",
    )
    op.drop_table("__APP_ID___sync_changes")
    op.drop_index(
        "ix___APP_ID___sync_entity_owner_updated",
        table_name="__APP_ID___sync_entities",
    )
    op.drop_table("__APP_ID___sync_entities")
